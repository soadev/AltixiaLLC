---
phase: 04-conversion-and-launch
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [src/components/FAQ.tsx, src/pages/Home.tsx]
autonomous: true
requirements: [FAQ-01, FAQ-02]

must_haves:
  truths:
    - FAQ section renders 10 questions in an accordion pattern
    - First question is open by default (initial state openIndex = 0)
    - Clicking a question smoothly expands/collapses it using Framer Motion AnimatePresence
    - Chevron icon rotates when question is open
    - All 10 questions and answers come from content data (useContent hook)
    - FAQ section displays correctly in FR and EN
  artifacts:
    - src/components/FAQ.tsx
  key_links:
    - FAQ.tsx uses useContent().faq (FAQContent type)
    - Home.tsx imports and renders FAQ after Testimonials
---

<objective>
Create the FAQ accordion section with 10 questions, AnimatePresence animations, and first-question-open-by-default behavior.

Purpose: Answer the top 10 questions visitors have about LLC creation, reducing friction before conversion.
Output: FAQ.tsx component wired into Home.tsx.
</objective>

<execution_context>
@/Users/enzomaillard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/enzomaillard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@src/types/content.ts
@src/hooks/useContent.ts
@src/data/content-fr.ts
@src/pages/Home.tsx
@design-system.md

<interfaces>
<!-- From src/types/content.ts -->
export interface FAQContent {
  sectionTitle: string
  items: { question: string; answer: string }[]
}

<!-- useContent() returns Content with faq: FAQContent -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FAQ accordion component via Gemini MCP</name>
  <files>src/components/FAQ.tsx</files>
  <action>
  **IMPORTANT: Use Gemini MCP for ALL visual/UI code. Claude NEVER writes JSX/Tailwind directly.**

  Call `mcp__gemini-design-mcp__create_frontend` with:
  - `prompt`: "Create a FAQ accordion section React component (FAQ.tsx) for a landing page. The section has a title (sectionTitle from data) and renders 10 question/answer items in an accordion pattern. Background color: #F7F9FC (off-white). Section title uses font-serif, centered. Each accordion item: question title in bold (font-semibold text-[#333A49]) with a chevron icon on the right that rotates 180 degrees when open. Answer text slides down smoothly using Framer Motion AnimatePresence with motion.div for the answer content. Border-bottom (#E8EDF2) between items. Question hover: text color transitions to #3877AF. Use React useState with initial state openIndex=0 so the first question is open by default. Clicking an already-open question closes it (set to null). The component receives data via useContent() hook returning { faq: { sectionTitle: string, items: { question: string, answer: string }[] } }. Import useContent from '../hooks/useContent'. Import { useState } from 'react'. Import { motion, AnimatePresence } from 'framer-motion'. For the chevron, use a simple SVG chevron-down icon inline (do not import from external icon library). The overall section should have max-w-3xl mx-auto for a narrower content width. Export as named export `FAQ`."
  - `designSystem`: THE ENTIRE content of design-system.md (full file, not summary)
  - `fileName`: "FAQ.tsx"
  - `filePath`: "src/components"

  **Post-generation review:**
  After Gemini generates the file, verify:
  - Imports `useContent` from `'../hooks/useContent'`
  - Uses `useState` with initial value `0` (not `null`) so first question is open
  - Uses `AnimatePresence` from `framer-motion` wrapping the answer content
  - Accesses `content.faq.sectionTitle` and `content.faq.items`
  - Named export `FAQ` (not default)
  - Fix any import/logic issues if needed (Claude handles logic, not visual)
  </action>
  <verify>
    <automated>cd "/Users/enzomaillard/Workspace_Local/Landing Pages/AltixiaLLC" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>FAQ.tsx renders 10 accordion items, first open by default, with AnimatePresence expand/collapse. Compiles without TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Wire FAQ into Home page</name>
  <files>src/pages/Home.tsx</files>
  <action>
  **Claude handles this (wiring, not visual).**

  Edit `src/pages/Home.tsx`:
  1. Add import: `import { FAQ } from '../components/FAQ'`
  2. Add after the Testimonials section:
     ```
     <section id="faq" className="bg-[#F7F9FC]">
       <FAQ />
     </section>
     ```
  Note: The section id="faq" is important for navbar scroll-to-section links.
  </action>
  <verify>
    <automated>cd "/Users/enzomaillard/Workspace_Local/Landing Pages/AltixiaLLC" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Home.tsx imports and renders FAQ after Testimonials. Section has id="faq" for anchor navigation. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- FAQ section visible with 10 questions
- First question is open by default
- Clicking questions expands/collapses with smooth animation
- Switching FR/EN updates all question and answer text
</verification>

<success_criteria>
- FAQ.tsx exists with AnimatePresence-powered accordion
- 10 questions rendered, first open by default
- Smooth expand/collapse animation on click
- Section anchored with id="faq" for navbar navigation
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-conversion-and-launch/04-02-SUMMARY.md`
</output>

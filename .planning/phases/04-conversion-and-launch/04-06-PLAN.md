---
phase: 04-conversion-and-launch
plan: 06
type: execute
wave: 6
depends_on: [05]
files_modified: [vercel.json]
autonomous: false
requirements: [TECH-04]

must_haves:
  truths:
    - vercel.json exists with SPA routing rewrites
    - npm run build succeeds without errors
    - Site deploys to Vercel and loads at production URL
    - Both FR and EN content visible on production
    - /llc route works on production (no 404)
  artifacts:
    - vercel.json
  key_links:
    - Build command: npm run build
    - Output directory: dist
    - SPA rewrite: all paths to /index.html
---

<objective>
Configure Vercel deployment and perform production smoke test.

Purpose: Ship the completed site to production so it is accessible at the Altixia domain.
Output: vercel.json config, successful build, and deployment verification.
</objective>

<execution_context>
@/Users/enzomaillard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/enzomaillard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create vercel.json and verify build</name>
  <files>vercel.json</files>
  <action>
  **Claude handles this (config, not visual).**

  **Step 1 — Create vercel.json:**
  Create `vercel.json` at project root:
  ```json
  {
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "rewrites": [
      { "source": "/(.*)", "destination": "/index.html" }
    ]
  }
  ```
  This ensures SPA routing works — /llc and any other route serve index.html.

  **Step 2 — Verify build succeeds:**
  ```bash
  cd "/Users/enzomaillard/Workspace_Local/Landing Pages/AltixiaLLC" && npm run build
  ```
  Build must complete without errors. Check that `dist/` contains `index.html` and asset files.

  **Step 3 — Verify dist output:**
  ```bash
  ls -la dist/
  ls -la dist/assets/
  ```
  Confirm index.html exists and JS/CSS assets are present.
  </action>
  <verify>
    <automated>cd "/Users/enzomaillard/Workspace_Local/Landing Pages/AltixiaLLC" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>vercel.json created with SPA routing config. `npm run build` succeeds and produces dist/ with all assets.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Deploy to Vercel and smoke test production</name>
  <files></files>
  <action>
  **This task requires human action for the actual Vercel deployment.**

  If the Vercel CLI is available, attempt deployment:
  ```bash
  cd "/Users/enzomaillard/Workspace_Local/Landing Pages/AltixiaLLC" && npx vercel --prod
  ```

  If Vercel CLI is not configured or deployment requires manual setup, present the checkpoint:

  The user needs to:
  1. Connect the repository to Vercel (via vercel.com dashboard or CLI)
  2. Deploy to production
  3. Verify:
     - Home page loads at production URL
     - All sections visible (Hero through Footer)
     - /llc page loads (no 404)
     - FR/EN toggle works
     - WhatsApp links functional
     - Mobile responsive layout correct

  **Smoke test checklist for user:**
  - [ ] Home page loads
  - [ ] All 14 sections visible in order
  - [ ] /llc page loads with state comparison
  - [ ] FR/EN switch works site-wide
  - [ ] WhatsApp button and links work
  - [ ] Mobile layout is correct
  - [ ] Page title updates on language switch
  </action>
  <verify>
    <automated>echo "Manual deployment verification required"</automated>
  </verify>
  <done>Site deployed to Vercel production. Home page and /llc page load correctly. FR/EN content works. All sections visible. Mobile responsive.</done>
</task>

</tasks>

<verification>
- vercel.json exists with correct SPA routing
- `npm run build` succeeds without errors
- Production site loads at Vercel URL
- Both / and /llc routes work
- FR/EN content renders correctly
</verification>

<success_criteria>
- vercel.json created with buildCommand, outputDirectory, and rewrites
- Build passes cleanly
- Site accessible at production URL
- All pages and sections functional
- Bilingual content working
</success_criteria>

<output>
After completion, create `.planning/phases/04-conversion-and-launch/04-06-SUMMARY.md`
</output>

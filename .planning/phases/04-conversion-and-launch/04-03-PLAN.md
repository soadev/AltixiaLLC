---
phase: 04-conversion-and-launch
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/components/FinalCTA.tsx, src/components/Footer.tsx, src/pages/Home.tsx, src/layouts/RootLayout.tsx]
autonomous: true
requirements: [CTA-01, FOOT-01]

must_haves:
  truths:
    - Final CTA section renders with H2 title, subtitle, two CTAs (Pack LLC primary + WhatsApp secondary), and reassurance text
    - Footer renders in 4 columns on desktop (logo+tagline, navigation, services, contact) on dark background #333A49
    - Footer shows bottom line with copyright and legal links
    - Footer is in RootLayout (shared between / and /llc pages)
    - Both sections display correctly in FR and EN
  artifacts:
    - src/components/FinalCTA.tsx
    - src/components/Footer.tsx
  key_links:
    - FinalCTA.tsx uses useContent().finalCta (FinalCTAContent type)
    - Footer.tsx uses useContent().footer (FooterContent type)
    - RootLayout.tsx renders Footer between </main> and WhatsAppButton
    - Home.tsx renders FinalCTA after FAQ
---

<objective>
Create the Final CTA and Footer sections, completing the bottom of the page.

Purpose: Final conversion push with clear CTAs, plus professional footer with navigation, services, and contact info.
Output: FinalCTA.tsx and Footer.tsx components. FinalCTA in Home.tsx, Footer in RootLayout.tsx (shared).
</objective>

<execution_context>
@/Users/enzomaillard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/enzomaillard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@src/types/content.ts
@src/hooks/useContent.ts
@src/data/content-fr.ts
@src/pages/Home.tsx
@src/layouts/RootLayout.tsx
@design-system.md

<interfaces>
<!-- From src/types/content.ts -->
export interface FinalCTAContent {
  title: string
  subtitle: string
  ctaPrimary: LinkItem
  ctaSecondary: LinkItem
  reassurance: string
}

export interface FooterContent {
  logo: string
  description: string
  columns: { title: string; links: LinkItem[] }[]
  contact: { phone: string; email: string; address: string }
  copyright: string
}

<!-- RootLayout.tsx currently: -->
<!-- Navbar + <main><Outlet /></main> + WhatsAppButton -->
<!-- Footer goes between </main> and <WhatsAppButton /> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FinalCTA and Footer components via Gemini MCP</name>
  <files>src/components/FinalCTA.tsx, src/components/Footer.tsx</files>
  <action>
  **IMPORTANT: Use Gemini MCP for ALL visual/UI code. Claude NEVER writes JSX/Tailwind directly.**

  **Step 1 — FinalCTA.tsx via `mcp__gemini-design-mcp__create_frontend`:**

  Call `mcp__gemini-design-mcp__create_frontend` with:
  - `prompt`: "Create a Final CTA section React component (FinalCTA.tsx) for a landing page. Background: light gradient from #D5E8F8 to white (or solid light #D5E8F8). Centered layout with: large H2 title (font-serif, #333A49), subtitle text (#6B7280), then two CTA buttons side by side on desktop / stacked on mobile. Primary CTA: full #3877AF button with white text, bold, rounded, shadow (the label and href come from data ctaPrimary). Secondary CTA: outline button with border, #333A49 text, includes a WhatsApp SVG icon (green #25D366) on the left (the label and href come from data ctaSecondary). Both buttons are <a> tags wrapping styled elements. Below the buttons: reassurance text in small muted text (#6B7280), centered. The reassurance text uses bullet separators. Generous py-24 spacing — this section should feel imposing and final. The component gets data via useContent() hook returning { finalCta: { title, subtitle, ctaPrimary: { label, href }, ctaSecondary: { label, href }, reassurance } }. Import useContent from '../hooks/useContent'. Use Framer Motion fade-in animation. Export as named export `FinalCTA`."
  - `designSystem`: THE ENTIRE content of design-system.md (full file, not summary)
  - `fileName`: "FinalCTA.tsx"
  - `filePath`: "src/components"

  **Step 2 — Footer.tsx via `mcp__gemini-design-mcp__create_frontend`:**

  Call `mcp__gemini-design-mcp__create_frontend` with:
  - `prompt`: "Create a Footer React component (Footer.tsx) for a landing page. Dark background #333A49, white and gray text. 4 columns on desktop, 2x2 on tablet, 1 column on mobile. Column 1: Logo text (font-bold text-white text-xl) + tagline/description text (text-sm text-white/60) + no actual image. Column 2: 'Navigation' title (text-white font-bold mb-4) + list of navigation links (text-white/60 hover:text-[#9BCAEF] transition-colors, text-sm). Column 3: 'Services' title + list of service links (same style). Column 4: 'Contact' title + phone number with WhatsApp icon, email address with Mail icon, physical address with MapPin icon (all in text-white/60). Use simple inline SVG icons or text emoji for contact icons. Below the 4 columns: a border-top separator (border-white/10) with the copyright text centered (text-white/40 text-xs). The component receives data via useContent() hook returning { footer: { logo, description, columns: [{ title, links: [{ label, href }] }], contact: { phone, email, address }, copyright } }. Import useContent from '../hooks/useContent'. Links use <a> tags. Export as named export `Footer`."
  - `designSystem`: THE ENTIRE content of design-system.md (full file, not summary)
  - `fileName`: "Footer.tsx"
  - `filePath`: "src/components"

  **Post-generation review:**
  - Both import `useContent` from `'../hooks/useContent'`
  - Both are named exports
  - FinalCTA accesses `content.finalCta.*`
  - Footer accesses `content.footer.*` including `content.footer.columns`, `content.footer.contact`
  - Fix any import/data access issues if needed
  </action>
  <verify>
    <automated>cd "/Users/enzomaillard/Workspace_Local/Landing Pages/AltixiaLLC" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>FinalCTA.tsx renders CTA section with two buttons and reassurance text. Footer.tsx renders 4-column footer on dark background. Both compile without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Wire FinalCTA into Home and Footer into RootLayout</name>
  <files>src/pages/Home.tsx, src/layouts/RootLayout.tsx</files>
  <action>
  **Claude handles this (wiring, not visual).**

  **Home.tsx:**
  1. Add import: `import { FinalCTA } from '../components/FinalCTA'`
  2. Add after the FAQ section:
     ```
     <section id="contact" className="bg-[#D5E8F8]">
       <FinalCTA />
     </section>
     ```
  Note: id="contact" for navbar anchor link.

  **RootLayout.tsx:**
  1. Add import: `import { Footer } from '../components/Footer'`
  2. Place `<Footer />` between `</main>` and `<WhatsAppButton />`:
     ```tsx
     <main>
       <Outlet />
     </main>
     <Footer />
     <WhatsAppButton />
     ```
  3. Remove the `{/* Footer will be added in Phase 4 */}` comment.
  </action>
  <verify>
    <automated>cd "/Users/enzomaillard/Workspace_Local/Landing Pages/AltixiaLLC" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>FinalCTA renders in Home.tsx after FAQ with id="contact". Footer renders in RootLayout shared between all pages. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- Final CTA section visible with two CTAs and reassurance text
- Footer visible at bottom of all pages (/ and /llc) with 4 columns
- Switching FR/EN updates all text in both sections
</verification>

<success_criteria>
- FinalCTA.tsx exists with dual CTAs and reassurance text
- Footer.tsx exists with 4-column layout on dark background
- Footer is shared via RootLayout (visible on both / and /llc)
- FinalCTA section has id="contact" for anchor navigation
- Zero TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-conversion-and-launch/04-03-SUMMARY.md`
</output>

---
phase: 04-conversion-and-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/Objections.tsx, src/components/Testimonials.tsx, src/pages/Home.tsx]
autonomous: true
requirements: [OBJ-01, TEST-01]

must_haves:
  truths:
    - Objections section renders 3 myth vs. reality blocks with X icon (red) for myth and checkmark icon (green) for reality
    - Testimonials section renders 3 quote cards with author attribution (role + location)
    - Both sections use real copy from content data (useContent hook), zero placeholders
    - Both sections display correctly in FR and EN
    - Home page shows Objections and Testimonials after Stats section
  artifacts:
    - src/components/Objections.tsx
    - src/components/Testimonials.tsx
  key_links:
    - Objections.tsx uses useContent().objections (ObjectionsContent type)
    - Testimonials.tsx uses useContent().testimonials (TestimonialsContent type)
    - Home.tsx imports and renders both components after Stats
---

<objective>
Create the Objections and Testimonials sections for the landing page.

Purpose: Handle visitor objections with a "myth vs reality" pattern and add social proof via client verbatim quotes.
Output: Objections.tsx and Testimonials.tsx components, wired into Home.tsx after Stats section.
</objective>

<execution_context>
@/Users/enzomaillard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/enzomaillard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@src/types/content.ts
@src/hooks/useContent.ts
@src/data/content-fr.ts
@src/pages/Home.tsx
@design-system.md

<interfaces>
<!-- From src/types/content.ts -->
export interface ObjectionsContent {
  sectionTitle: string
  items: { myth: string; reality: string }[]
}

export interface TestimonialsContent {
  sectionTitle: string
  items: { quote: string; author: string; role: string; location: string }[]
}

<!-- From src/hooks/useContent.ts -->
export function useContent(): Content  // returns full Content object based on language

<!-- Pattern from existing components -->
import { useContent } from '../hooks/useContent'
import { motion } from 'framer-motion'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Objections and Testimonials components via Gemini MCP</name>
  <files>src/components/Objections.tsx, src/components/Testimonials.tsx</files>
  <action>
  **IMPORTANT: Use Gemini MCP for ALL visual/UI code. Claude NEVER writes JSX/Tailwind directly.**

  **Step 1 — Objections.tsx via `mcp__gemini-design-mcp__create_frontend`:**

  Call `mcp__gemini-design-mcp__create_frontend` with:
  - `prompt`: "Create an Objections section React component (Objections.tsx) for a landing page. The section has a title (sectionTitle) and renders 3 myth vs reality blocks. Design pattern: side-by-side layout — left side shows the myth/objection text in a muted style with a red X icon (#EF4444), right side shows the reality/response with a green checkmark icon (#10B981). Cards have white background, subtle border (#E8EDF2), shadow on hover. 3 blocks stacked vertically with generous spacing. White section background. Section title uses font-serif. The component receives data via a `useContent()` hook that returns `{ objections: { sectionTitle: string, items: { myth: string, reality: string }[] } }`. Import useContent from '../hooks/useContent'. Use Framer Motion for fade-in + slide-up scroll animations (initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }}). Export as named export `Objections`."
  - `designSystem`: THE ENTIRE content of design-system.md (full file, not summary)
  - `fileName`: "Objections.tsx"
  - `filePath`: "src/components"

  **Step 2 — Testimonials.tsx via `mcp__gemini-design-mcp__create_frontend`:**

  Call `mcp__gemini-design-mcp__create_frontend` with:
  - `prompt`: "Create a Testimonials section React component (Testimonials.tsx) for a landing page. The section has a title (sectionTitle) and renders 3 client quote cards in a row on desktop, stacked on mobile. Each card has: a large decorative opening quotation mark in #D5E8F8 or #9BCAEF as visual anchor, the quote text in italic, and attribution below showing role + location (e.g. 'E-commerce — France'). No photos. White background section. Below the 3 cards, add a centered closing text line: use the text 'Ils avaient les memes doutes que vous. Aujourd'hui, leur LLC est active.' as a static text (hardcoded in FR, this will be localized later). Cards have white bg, subtle border, rounded corners, subtle shadow. The component receives data via `useContent()` hook returning `{ testimonials: { sectionTitle: string, items: { quote: string, author: string, role: string, location: string }[] } }`. Import useContent from '../hooks/useContent'. Use Framer Motion for staggered fade-in animations. Export as named export `Testimonials`."
  - `designSystem`: THE ENTIRE content of design-system.md (full file, not summary)
  - `fileName`: "Testimonials.tsx"
  - `filePath`: "src/components"

  **Step 3 — Post-generation review:**
  After Gemini generates both files, verify:
  - Both import `useContent` from `'../hooks/useContent'`
  - Both use `motion` from `framer-motion`
  - Both are named exports (not default exports)
  - Objections accesses `content.objections.sectionTitle` and `content.objections.items`
  - Testimonials accesses `content.testimonials.sectionTitle` and `content.testimonials.items`
  - Fix any import issues or data access patterns if needed (Claude handles logic fixes, not visual)
  </action>
  <verify>
    <automated>cd "/Users/enzomaillard/Workspace_Local/Landing Pages/AltixiaLLC" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Objections.tsx renders 3 myth/reality blocks with red X and green check icons. Testimonials.tsx renders 3 quote cards with attribution. Both compile without TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Objections and Testimonials into Home page</name>
  <files>src/pages/Home.tsx</files>
  <action>
  **Claude handles this (routing/wiring, not visual).**

  Edit `src/pages/Home.tsx`:
  1. Add imports at the top:
     ```
     import { Objections } from '../components/Objections'
     import { Testimonials } from '../components/Testimonials'
     ```
  2. Add after the Stats section (after `</section>` that wraps `<Stats />`), before the closing `</div>`:
     ```
     <section id="objections" className="bg-white">
       <Objections />
     </section>
     <section className="bg-white">
       <Testimonials />
     </section>
     ```
  3. Remove the `{/* More sections added in Phase 4 */}` comment.
  </action>
  <verify>
    <automated>cd "/Users/enzomaillard/Workspace_Local/Landing Pages/AltixiaLLC" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Home.tsx imports and renders Objections and Testimonials after Stats. TypeScript compiles cleanly. Page shows the new sections in correct order.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- Objections section visible on page with 3 myth/reality blocks
- Testimonials section visible with 3 quote cards and attribution
- Switching FR/EN updates all text in both sections
</verification>

<success_criteria>
- Objections.tsx exists and renders 3 myth vs reality blocks with correct icons
- Testimonials.tsx exists and renders 3 quote cards with author/role/location
- Both sections appear on home page after Stats section
- Zero TypeScript compilation errors
- All text comes from content data files (no hardcoded copy in components)
</success_criteria>

<output>
After completion, create `.planning/phases/04-conversion-and-launch/04-01-SUMMARY.md`
</output>

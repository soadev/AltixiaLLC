---
phase: 04-conversion-and-launch
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified: [package.json, src/main.tsx, src/components/SEOHead.tsx, src/pages/Home.tsx, src/pages/LLCPage.tsx]
autonomous: true
requirements: [TECH-04]

must_haves:
  truths:
    - SEO meta tags present in HTML head for both / and /llc routes
    - Open Graph tags (og:title, og:description, og:type, og:url) present
    - Page title changes dynamically based on active language (FR/EN)
    - Different meta content for / and /llc routes
    - Meta description matches hero/page subtitle content
  artifacts:
    - src/components/SEOHead.tsx
  key_links:
    - SEOHead uses useLanguage() for dynamic language-based meta
    - Home.tsx and LLCPage.tsx include SEOHead with route-specific props
---

<objective>
Add SEO meta tags, Open Graph tags, and dynamic page titles to all routes.

Purpose: Ensure the site is discoverable and shares well on social media with correct titles and descriptions.
Output: SEOHead component with route-specific meta, integrated into both pages.
</objective>

<execution_context>
@/Users/enzomaillard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/enzomaillard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@src/main.tsx
@src/pages/Home.tsx
@src/pages/LLCPage.tsx
@src/contexts/LanguageContext.tsx

<interfaces>
<!-- From src/contexts/LanguageContext.tsx -->
export function useLanguage(): { language: Language; setLanguage: (lang: Language) => void }
<!-- Language = 'fr' | 'en' -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-helmet-async and create SEOHead component</name>
  <files>package.json, src/main.tsx, src/components/SEOHead.tsx</files>
  <action>
  **Claude handles this (logic/config, not visual UI).**

  **Step 1 — Install dependency:**
  ```bash
  cd "/Users/enzomaillard/Workspace_Local/Landing Pages/AltixiaLLC" && npm install react-helmet-async
  ```

  **Step 2 — Wrap App with HelmetProvider in main.tsx:**
  Edit `src/main.tsx` to import `HelmetProvider` from `react-helmet-async` and wrap the `<App />` component:
  ```tsx
  import { HelmetProvider } from 'react-helmet-async'
  // ... existing imports

  createRoot(document.getElementById('root')!).render(
    <StrictMode>
      <HelmetProvider>
        <App />
      </HelmetProvider>
    </StrictMode>
  )
  ```

  **Step 3 — Create SEOHead.tsx:**
  Create `src/components/SEOHead.tsx` — this is a logic-only component (meta tags, no visual UI), so Claude writes it directly:

  ```tsx
  import { Helmet } from 'react-helmet-async'
  import { useLanguage } from '../contexts/LanguageContext'

  interface SEOHeadProps {
    titleFr: string
    titleEn: string
    descriptionFr: string
    descriptionEn: string
    path: string
  }

  export function SEOHead({ titleFr, titleEn, descriptionFr, descriptionEn, path }: SEOHeadProps) {
    const { language } = useLanguage()
    const title = language === 'fr' ? titleFr : titleEn
    const description = language === 'fr' ? descriptionFr : descriptionEn
    const url = `https://altixia.com${path}`

    return (
      <Helmet>
        <html lang={language} />
        <title>{title}</title>
        <meta name="description" content={description} />
        <meta property="og:title" content={title} />
        <meta property="og:description" content={description} />
        <meta property="og:type" content="website" />
        <meta property="og:url" content={url} />
        <meta property="og:site_name" content="Altixia LLC" />
        <link rel="canonical" href={url} />
      </Helmet>
    )
  }
  ```
  </action>
  <verify>
    <automated>cd "/Users/enzomaillard/Workspace_Local/Landing Pages/AltixiaLLC" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>react-helmet-async installed, HelmetProvider wraps App, SEOHead component created with dynamic language-based meta tags.</done>
</task>

<task type="auto">
  <name>Task 2: Add SEOHead to Home and LLC pages</name>
  <files>src/pages/Home.tsx, src/pages/LLCPage.tsx</files>
  <action>
  **Claude handles this (wiring, not visual).**

  **Home.tsx:**
  1. Add import: `import { SEOHead } from '../components/SEOHead'`
  2. Add `<SEOHead />` as the first child inside the wrapping `<div>`:
     ```tsx
     <SEOHead
       titleFr="Altixia LLC — Votre LLC américaine clé en main"
       titleEn="Altixia LLC — Your US LLC, turnkey"
       descriptionFr="Altixia accompagne les expatriés francophones dans la création de leur LLC américaine. LLC, EIN, ITIN, compte bancaire, comptabilité — tout est géré, en français."
       descriptionEn="Altixia helps French-speaking expats set up their US LLC. LLC, EIN, ITIN, bank account, accounting — everything managed, in your language."
       path="/"
     />
     ```

  **LLCPage.tsx:**
  1. Add import: `import { SEOHead } from '../components/SEOHead'`
  2. Add `<SEOHead />` as the first child inside the wrapping element:
     ```tsx
     <SEOHead
       titleFr="Création de LLC aux États-Unis — Wyoming, Delaware, Nouveau-Mexique | Altixia"
       titleEn="US LLC Formation — Wyoming, Delaware, New Mexico | Altixia"
       descriptionFr="Créez votre LLC américaine au Wyoming, Delaware ou Nouveau-Mexique. Pack complet : LLC + EIN + ITIN + compte bancaire. En français, en 48h."
       descriptionEn="Create your US LLC in Wyoming, Delaware, or New Mexico. Complete pack: LLC + EIN + ITIN + bank account. In your language, in 48h."
       path="/llc"
     />
     ```
  </action>
  <verify>
    <automated>cd "/Users/enzomaillard/Workspace_Local/Landing Pages/AltixiaLLC" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Both pages include SEOHead with route-specific titles and descriptions. Meta tags render dynamically based on language. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- View page source: `<title>` tag contains correct language-specific title
- Meta description and OG tags present in head
- Switching language updates title and meta
- Different meta content on / vs /llc
</verification>

<success_criteria>
- react-helmet-async installed and HelmetProvider wrapping App
- SEOHead component renders dynamic meta based on language
- Home page has FR/EN titles and descriptions
- LLC page has different FR/EN titles and descriptions
- OG tags present (og:title, og:description, og:type, og:url)
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-conversion-and-launch/04-05-SUMMARY.md`
</output>

---
phase: 04-conversion-and-launch
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified: [src/pages/LLCPage.tsx]
autonomous: true
requirements: [LLC-01, LLC-02, LLC-03]

must_haves:
  truths:
    - /llc page renders with shared navbar and footer (via RootLayout)
    - LLC-specific hero with title and subtitle from content data
    - State comparison section shows 3 cards (Wyoming, Delaware, New Mexico) with pros listed
    - Pack inclusion checklist with 7 items
    - Dual CTAs (Commander ma LLC + WhatsApp)
    - All content bilingual FR/EN via useContent().llcPage
  artifacts:
    - src/pages/LLCPage.tsx (rewritten with full content)
  key_links:
    - LLCPage.tsx uses useContent().llcPage (LLCPageContent type)
    - Shared Navbar/Footer via RootLayout (from plan 04-03)
---

<objective>
Build the complete /llc dedicated page with state comparison, pack checklist, and CTAs.

Purpose: The /llc URL appears on official client documents — it must be a complete, professional page showing LLC formation details for Wyoming, Delaware, and New Mexico.
Output: Full LLCPage.tsx replacing the current placeholder.
</objective>

<execution_context>
@/Users/enzomaillard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/enzomaillard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@src/types/content.ts
@src/hooks/useContent.ts
@src/data/content-fr.ts
@src/pages/LLCPage.tsx
@design-system.md

<interfaces>
<!-- From src/types/content.ts -->
export interface LLCPageContent {
  title: string
  subtitle: string
  states: { name: string; pros: string[] }[]
  checklist: string[]
  ctaPrimary: LinkItem
  ctaSecondary: LinkItem
}

<!-- LinkItem = { label: string; href: string } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build complete /llc page via Gemini MCP</name>
  <files>src/pages/LLCPage.tsx</files>
  <action>
  **IMPORTANT: Use Gemini MCP for ALL visual/UI code. Claude NEVER writes JSX/Tailwind directly.**

  Call `mcp__gemini-design-mcp__create_frontend` with:
  - `prompt`: "Create a full LLC landing page React component (LLCPage.tsx) with multiple sections. This is a dedicated page (not a section of the home page). It will be wrapped by a shared layout that already provides Navbar and Footer, so do NOT include those.

  The page has these sections, top to bottom:

  1. **LLC Hero section**: Background #D5E8F8 (light blue). Centered text: Large H1 title (font-serif, #333A49), subtitle text (#6B7280) below. Generous py-20 padding.

  2. **State Comparison section**: White background, py-20. Section heading 'Comparez les Etats' (or similar, this is visual-only heading). 3 cards side by side on desktop, stacked on mobile. Each card: white bg, border #E8EDF2, rounded-lg, p-8. Card header: state name in font-serif text-xl font-bold #333A49. Below: list of pros with green checkmark (#10B981) icon before each item. The data comes from content.llcPage.states array (3 items, each with name and pros array).

  3. **Pack Checklist section**: Background #F7F9FC. py-20. Centered heading 'Tout est inclus dans le Pack LLC'. A vertical checklist with 7 items, each with a green checkmark icon and the item text. Clean, generous spacing between items. Max-w-2xl centered.

  4. **CTA section**: White background, py-20. Centered: two buttons side by side on desktop, stacked on mobile. Primary button: bg-[#3877AF] text-white rounded font-bold shadow. Secondary button: outline style with border, includes WhatsApp icon. Data from content.llcPage.ctaPrimary and ctaSecondary (each has label and href). Both are <a> tags.

  The component receives all data via useContent() hook returning { llcPage: { title, subtitle, states: [{ name, pros: string[] }], checklist: string[], ctaPrimary: { label, href }, ctaSecondary: { label, href } } }. Import useContent from '../hooks/useContent'. Use Framer Motion for fade-in scroll animations on each section. Export as named export `LLCPage`."
  - `designSystem`: THE ENTIRE content of design-system.md (full file, not summary)
  - `fileName`: "LLCPage.tsx"
  - `filePath`: "src/pages"

  **Post-generation review:**
  - Imports `useContent` from `'../hooks/useContent'`
  - Named export `LLCPage`
  - Accesses `content.llcPage.title`, `content.llcPage.subtitle`, `content.llcPage.states`, `content.llcPage.checklist`, `content.llcPage.ctaPrimary`, `content.llcPage.ctaSecondary`
  - Does NOT include Navbar or Footer (those are in RootLayout)
  - Fix any import/data access issues
  </action>
  <verify>
    <automated>cd "/Users/enzomaillard/Workspace_Local/Landing Pages/AltixiaLLC" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>LLCPage.tsx renders complete /llc page with hero, state comparison (3 cards), pack checklist, and dual CTAs. TypeScript compiles cleanly. No Navbar/Footer included (handled by RootLayout).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- Navigating to /llc shows the full page content
- 3 state cards visible with Wyoming, Delaware, New Mexico pros
- Pack checklist shows 7 items
- CTAs visible with correct links
- Switching FR/EN updates all text
- Navbar and Footer appear (from RootLayout)
</verification>

<success_criteria>
- LLCPage.tsx exists with full content (no placeholder)
- State comparison shows 3 cards with pros
- Pack checklist renders 7 items
- Two CTAs link to WhatsApp
- Page shares Navbar/Footer via RootLayout
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-conversion-and-launch/04-04-SUMMARY.md`
</output>

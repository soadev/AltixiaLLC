---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - design-system.md
autonomous: true
requirements: [FOUND-02, TECH-07]
must_haves:
  truths:
    - "design-system.md exists at project root with full design system code"
    - "Design system contains color tokens matching the brand palette (#3877AF, #D5E8F8, #333A49, #9BCAEF)"
    - "Design system specifies serif font for titles and sans-serif for body"
    - "Design system includes component patterns (buttons, cards, sections)"
  artifacts:
    - path: "design-system.md"
      provides: "Complete design system for all Gemini MCP calls"
      min_lines: 50
  key_links:
    - from: "design-system.md"
      to: "All future Gemini MCP calls"
      via: "designSystem parameter"
      pattern: "#3877AF"
---

<objective>
Generate the project design system via Gemini MCP generate_vibes and save as design-system.md.

Purpose: Every future Gemini MCP frontend call requires the full design-system.md content. This is the design foundation for the entire project.
Output: design-system.md at project root with complete design system code (colors, typography, spacing, component tokens).
</objective>

<execution_context>
@/Users/enzomaillard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/enzomaillard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@BRIEF.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate design system via Gemini MCP generate_vibes</name>
  <files>design-system.md</files>
  <action>
  **CRITICAL: This task MUST use the Gemini MCP tool `mcp__gemini-design-mcp__create_frontend` with a generate_vibes prompt. Claude NEVER writes design system code directly.**

  1. First, use ToolSearch to load `mcp__gemini-design-mcp__create_frontend` tool.

  2. Call `mcp__gemini-design-mcp__create_frontend` with these parameters:
     - **fileName**: `vibes-selection.tsx`
     - **projectDescription**: `Landing page ultra-professionnelle pour Altixia LLC, une société immatriculée au Wyoming (USA) qui accompagne les expatriés francophones (France, Belgique, Suisse) dans la création de LLC américaines, l'obtention d'ITIN, la comptabilité US, l'ouverture de comptes bancaires, et l'expatriation fiscale internationale. Le site doit inspirer CONFIANCE, EXPERTISE et SÉRIEUX — c'est un cabinet de conseil international moderne, pas une startup fun. Palette : fond blanc dominant, bleu primaire #3877AF, bleu clair #D5E8F8, bleu-noir #333A49, bleu médium #9BCAEF. Typo élégante et pro (pas Inter/Roboto/Arial). Style inspiré des meilleurs sites de legal-tech, fintech, ou cabinets de conseil premium. Animations subtiles et élégantes au scroll. Le site est bilingue FR/EN.`
     - **techStack**: `Vite 6, React 19, TypeScript, Tailwind CSS 4, Framer Motion`
     - **context**: `Generate a design system with 5 different visual vibes for a premium international legal/financial advisory firm landing page. Must include: full color palette based on #3877AF/#D5E8F8/#333A49/#9BCAEF, serif font for titles (DM Serif Display or Playfair Display or Fraunces or Lora — NEVER Inter/Roboto/Arial), modern sans-serif for body (DM Sans or Plus Jakarta Sans or Outfit or Manrope), spacing scale, component tokens for buttons/cards/sections, responsive breakpoints, animation tokens for Framer Motion. Each vibe should show a hero section, a card grid, a CTA section, a stats section, and a footer preview. Mark each vibe with <!-- VIBE_X_START --> and <!-- VIBE_X_END --> comments.`

  3. Read the generated `vibes-selection.tsx` file.

  4. Auto-select the MOST premium, professional, trust-inspiring vibe (per CONTEXT.md decision: "Auto-select the most premium/professional/trust-inspiring vibe (no user prompt)"). Look for the vibe that best conveys:
     - International advisory firm gravitas
     - Clean, elegant serif/sans-serif typography pairing
     - Subtle, professional color usage
     - Premium spacing and layout

  5. Extract the COMPLETE code between the selected `<!-- VIBE_X_START -->` and `<!-- VIBE_X_END -->` markers.

  6. Save the extracted code as `design-system.md` at the project root. The file should contain the full design system code — NOT a summary, NOT extracted tokens, but the ENTIRE vibe code.

  7. Delete `vibes-selection.tsx` after extraction.

  **FALLBACK:** If generate_vibes is not available as a separate tool, use `create_frontend` with the above context to generate a design system page, then extract the best vibe.
  </action>
  <verify>
    <automated>test -f design-system.md && grep -q "#3877AF" design-system.md && grep -q "#333A49" design-system.md && wc -l design-system.md | awk '{if ($1 > 50) print "DESIGN SYSTEM OK (" $1 " lines)"; else print "DESIGN SYSTEM TOO SHORT"}'</automated>
  </verify>
  <done>
  - design-system.md exists at project root
  - Contains the complete code of the selected vibe (not a summary)
  - Includes brand colors (#3877AF, #D5E8F8, #333A49, #9BCAEF)
  - Includes serif font for titles and sans-serif for body
  - Includes component patterns (buttons, cards, sections)
  - vibes-selection.tsx is deleted
  - File is at least 50 lines (full design system, not a stub)
  </done>
</task>

</tasks>

<verification>
- `test -f design-system.md` succeeds
- `grep "#3877AF" design-system.md` finds the primary color
- `grep "#D5E8F8" design-system.md` finds the light blue
- `grep "#333A49" design-system.md` finds the dark navy
- `test ! -f vibes-selection.tsx` confirms cleanup
- File has substantial content (>50 lines)
</verification>

<success_criteria>
- design-system.md exists at project root with full design system code
- All brand colors are present
- Typography uses serif + sans-serif pairing (not Inter/Roboto/Arial)
- vibes-selection.tsx is removed
- design-system.md is ready to be passed as designSystem parameter to all future Gemini MCP calls
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>

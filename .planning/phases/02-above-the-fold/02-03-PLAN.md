---
phase: 02-above-the-fold
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/components/TrustBar.tsx
  - src/components/WhatsAppButton.tsx
  - src/pages/Home.tsx
  - src/layouts/RootLayout.tsx
autonomous: true
requirements: [TRUST-01, TECH-02]

must_haves:
  truths:
    - "Trust bar shows all 5 reassurance elements in a horizontal row below the hero"
    - "Each trust bar item has a Lucide icon and descriptive text with bold keywords in blue"
    - "Trust bar items have staggered fade-in animation when scrolled into view"
    - "Floating WhatsApp button is always visible at bottom-right corner"
    - "WhatsApp button links to https://wa.me/13073105297"
    - "WhatsApp button has a subtle pulse animation"
    - "Trust bar text changes when language is toggled"
  artifacts:
    - path: "src/components/TrustBar.tsx"
      provides: "Horizontal trust bar with 5 reassurance items"
    - path: "src/components/WhatsAppButton.tsx"
      provides: "Fixed floating WhatsApp contact button"
    - path: "src/pages/Home.tsx"
      provides: "Home page with Hero + TrustBar sections"
    - path: "src/layouts/RootLayout.tsx"
      provides: "Layout with Navbar + WhatsAppButton"
  key_links:
    - from: "src/components/TrustBar.tsx"
      to: "src/hooks/useContent.ts"
      via: "useContent() for bilingual trust bar items"
      pattern: "useContent\\(\\)"
    - from: "src/components/TrustBar.tsx"
      to: "src/components/LucideIcon.tsx"
      via: "LucideIcon for item icons"
      pattern: "<LucideIcon"
    - from: "src/pages/Home.tsx"
      to: "src/components/TrustBar.tsx"
      via: "import and render below Hero"
      pattern: "<TrustBar"
    - from: "src/layouts/RootLayout.tsx"
      to: "src/components/WhatsAppButton.tsx"
      via: "import and render as fixed element"
      pattern: "<WhatsAppButton"
---

<objective>
Build the trust bar with 5 reassurance elements and the floating WhatsApp button, then complete the Home page and RootLayout assembly.

Purpose: The trust bar reinforces key selling points (speed, reliability, French support, location, availability) immediately after the hero. The WhatsApp button provides an always-available conversion path on every page.
Output: TrustBar.tsx and WhatsAppButton.tsx via Gemini MCP, Home.tsx and RootLayout.tsx updated with final component assembly.
</objective>

<execution_context>
@/Users/enzomaillard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/enzomaillard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@design-system.md
@src/hooks/useContent.ts
@src/types/content.ts
@src/data/content-fr.ts
@src/pages/Home.tsx
@src/layouts/RootLayout.tsx
@BRIEF.md

<interfaces>
From src/types/content.ts:
```typescript
export interface TrustBarContent {
  items: { icon: string; text: string }[]
}
```

From src/data/content-fr.ts (trustBar section):
```typescript
trustBar: {
  items: [
    { icon: 'Clock', text: 'LLC créée en 48h' },
    { icon: 'CheckCircle', text: '100% de réussite' },
    { icon: 'Globe', text: 'Accompagnement en français' },
    { icon: 'MapPin', text: 'Basé au Wyoming, USA' },
    { icon: 'MessageCircle', text: 'Support WhatsApp 7j/7' },
  ],
}
```

From src/components/LucideIcon.tsx (created in Plan 01):
```typescript
export function LucideIcon({ name, size, className }: { name: string; size?: number; className?: string }): JSX.Element | null
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TrustBar and WhatsAppButton via Gemini MCP</name>
  <files>src/components/TrustBar.tsx, src/components/WhatsAppButton.tsx</files>
  <action>
1. Use Gemini MCP `create_frontend` to generate the TrustBar component:
   - `designSystem`: Pass the ENTIRE content of `design-system.md` (all 230 lines)
   - `context`: Include ALL of the following:
     * "Build a compact horizontal trust/reassurance bar for Altixia LLC landing page"
     * "Full-width band with bg-[#F7F9FC] py-8 px-8, below the hero section"
     * "max-w-6xl mx-auto container"
     * "5 items in a horizontal row (flex justify-between or grid grid-cols-5): each item has a Lucide icon in text-[#3877AF] and descriptive text in text-[#6B7280] text-sm"
     * "Items: Clock 'LLC créée en 48h', CheckCircle '100% de réussite', Globe 'Accompagnement en français', MapPin 'Basé au Wyoming, USA', MessageCircle 'Support WhatsApp 7j/7'"
     * "Bold keywords highlighted: '48h', '100%', 'français', 'Wyoming', 'WhatsApp' should be in font-bold text-[#3877AF]"
     * "Each item layout: icon above or left of text, centered alignment"
     * "Mobile responsive: wrap to 2-3 per row using flex-wrap or grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 with gap-6"
     * "Compact strip — NOT a full section. No section title, no subtitle. Just the 5 quick trust signals."
     * "Framer Motion: staggered fade-in when scrolled into view using whileInView. Each item fades in with 0.1s stagger."
     * "Fonts: DM Sans for all text"
   - `framework`: "react"
   - `fileName`: "TrustBar"

2. After receiving Gemini output for TrustBar, wire the logic:
   - Import and use `useContent()` to get `content.trustBar`
   - Import `LucideIcon` from `./LucideIcon`
   - Map `content.trustBar.items` to render each item with `<LucideIcon name={item.icon} />`
   - For bold keywords: use a regex or split approach to wrap specific words (48h, 100%, français/French, Wyoming, WhatsApp) in `<span className="font-bold text-[#3877AF]">`. A simple approach: for each item text, find the keyword and wrap it.
   - Use Framer Motion `motion.div` with `whileInView` for stagger animation

3. Use Gemini MCP `create_frontend` to generate the WhatsAppButton component:
   - `designSystem`: Pass the ENTIRE content of `design-system.md`
   - `context`: Include ALL of the following:
     * "Build a floating WhatsApp contact button for bottom-right corner"
     * "Fixed position: bottom-6 right-6 on desktop, bottom-4 right-4 on mobile"
     * "Green circle: w-14 h-14 bg-[#25D366] rounded-full shadow-lg flex items-center justify-center"
     * "White WhatsApp icon inside (use MessageCircle from lucide-react as placeholder, or an SVG WhatsApp icon)"
     * "Links to https://wa.me/13073105297 — opens in new tab"
     * "Z-index: z-50 (above everything except modals)"
     * "Subtle pulse animation: use CSS animate-pulse or a custom Framer Motion animation with scale 1.0 to 1.05 repeating"
     * "Shadow: shadow-lg shadow-[#25D366]/30 for green glow effect"
     * "Hover: scale up slightly (scale: 1.1) with transition"
   - `framework`: "react"
   - `fileName`: "WhatsAppButton"

4. After receiving Gemini output for WhatsAppButton:
   - Ensure it renders as an `<a>` tag with `href="https://wa.me/13073105297"` and `target="_blank" rel="noopener noreferrer"`
   - If Gemini used MessageCircle as placeholder, optionally replace with an inline SVG of the WhatsApp logo for better brand recognition
   - Verify z-50 is set for proper stacking
  </action>
  <verify>
    <automated>cd /Users/enzomaillard/Workspace_Local/Landing\ Pages/AltixiaLLC && npx tsc --noEmit src/components/TrustBar.tsx src/components/WhatsAppButton.tsx 2>&1 | head -20</automated>
  </verify>
  <done>TrustBar renders 5 items with Lucide icons and bilingual text. WhatsAppButton renders as fixed green circle at bottom-right linking to wa.me. Both compile without TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble TrustBar in Home and WhatsAppButton in RootLayout</name>
  <files>src/pages/Home.tsx, src/layouts/RootLayout.tsx</files>
  <action>
1. Update `src/pages/Home.tsx`:
   - Import `TrustBar` from `../components/TrustBar`
   - Add `<TrustBar />` below the `<section id="accueil">` that wraps Hero
   - Structure:
     ```tsx
     <div>
       <section id="accueil">
         <Hero />
       </section>
       <TrustBar />
       {/* More sections added in Phase 3+ */}
     </div>
     ```

2. Update `src/layouts/RootLayout.tsx`:
   - Import `WhatsAppButton` from `../components/WhatsAppButton`
   - Add `<WhatsAppButton />` after `</main>` and before the footer comment
   - Final structure:
     ```tsx
     <div className="min-h-screen font-sans text-dark">
       <Navbar />
       <main>
         <Outlet />
       </main>
       <WhatsAppButton />
       {/* Footer will be added in Phase 4 */}
     </div>
     ```
  </action>
  <verify>
    <automated>cd /Users/enzomaillard/Workspace_Local/Landing\ Pages/AltixiaLLC && npx tsc --noEmit src/pages/Home.tsx src/layouts/RootLayout.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Home page renders Hero + TrustBar in correct order. RootLayout renders Navbar + page content + WhatsAppButton. WhatsApp button visible on all pages. No TypeScript errors.</done>
</task>

</tasks>

<verification>
- `npm run dev` starts and full above-the-fold experience is visible
- Trust bar shows below hero with 5 items, each with icon and text
- Bold keywords highlighted in blue within trust bar items
- Trust bar stagger animation plays when scrolled into view
- WhatsApp button visible at bottom-right, green circle with icon
- WhatsApp button has subtle pulse animation
- Clicking WhatsApp button opens wa.me link
- WhatsApp button visible on /llc page too (it's in RootLayout)
- Toggling FR/EN updates trust bar text
- Mobile: trust bar items wrap to multiple rows
</verification>

<success_criteria>
- TrustBar created via Gemini MCP with 5 reassurance items
- WhatsAppButton created via Gemini MCP as fixed floating element
- Bold keyword highlighting in trust bar
- Staggered animation on trust bar items
- WhatsApp button with pulse animation and z-50
- Home page: Hero + TrustBar assembled
- RootLayout: Navbar + WhatsAppButton assembled
- All text bilingual via useContent()
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-above-the-fold/02-03-SUMMARY.md`
</output>

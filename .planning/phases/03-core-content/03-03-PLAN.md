---
phase: 03-core-content
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/PackLLC.tsx
autonomous: true
requirements: [PACK-01, PACK-02, PACK-03]

must_haves:
  truths:
    - "Pack LLC section has a light blue accent background to visually stand out"
    - "7-item checklist displays with green checkmark icons"
    - "Primary CTA button 'Commander mon Pack LLC' links to WhatsApp"
    - "Secondary WhatsApp link below the CTA"
    - "Callout note explains why no standalone LLC with soft yellow/amber info box styling"
    - "No price is displayed anywhere in the section"
    - "All text is bilingual via useContent() hook"
  artifacts:
    - path: "src/components/PackLLC.tsx"
      provides: "Pack LLC section with checklist, CTAs, and callout"
      min_lines: 50
  key_links:
    - from: "src/components/PackLLC.tsx"
      to: "src/hooks/useContent.ts"
      via: "useContent() hook for bilingual text"
      pattern: "useContent\\(\\)"
---

<objective>
Build the Pack LLC section — the main conversion section — with a 7-item checklist, primary CTA, WhatsApp link, and an informational callout explaining why no standalone LLC is offered.

Purpose: This is the key conversion driver. The accent background makes it visually distinct from surrounding sections, the checklist shows comprehensive value, and the callout preempts the "why can't I just get an LLC?" objection. CTAs drive to WhatsApp for pricing discussions.
Output: PackLLC.tsx component via Gemini MCP with scroll animations and bilingual content.
</objective>

<execution_context>
@/Users/enzomaillard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/enzomaillard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@design-system.md
@src/types/content.ts
@src/hooks/useContent.ts
@src/components/Hero.tsx
@src/data/content-fr.ts
@BRIEF.md

<interfaces>
From src/types/content.ts:
```typescript
export interface PackLLCContent {
  sectionTitle: string
  subtitle: string
  checklist: string[]
  ctaPrimary: LinkItem
  ctaSecondary: LinkItem
  callout: string
}

export interface LinkItem {
  label: string
  href: string
}
```

From src/hooks/useContent.ts:
```typescript
export function useContent(): Content
// Access via: content.packLLC.sectionTitle, content.packLLC.checklist, content.packLLC.callout
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Pack LLC section via Gemini MCP</name>
  <files>src/components/PackLLC.tsx</files>
  <action>
1. Read `design-system.md` fully (all content, verbatim).

2. Use Gemini MCP `create_frontend` to generate the PackLLC component:
   - `designSystem`: Pass the ENTIRE content of `design-system.md`
   - `context`: Include ALL of the following:
     * "Build a Pack LLC section for Altixia LLC landing page — this is the MAIN conversion section"
     * "Section background: light brand-blue tint bg-[#D5E8F8]/20 or bg-[#EBF3FA] to visually distinguish from other sections"
     * "Section has a centered header: section title (DM Serif Display, text-[#333A49], text-3xl md:text-4xl font-bold) + subtitle (DM Sans, text-[#6B7280], max-w-2xl mx-auto text-lg)"
     * "Main content area: centered max-w-2xl, two-column layout on desktop (checklist left, CTAs+callout right) or single-column stacked"
     * "Checklist: 7 items stacked vertically, each with a green CheckCircle2 icon (text-[#10B981]) + text (text-[#333A49] font-medium). Space-y-4."
     * "CTAs below checklist: Primary button (bg-[#3877AF] text-white px-8 py-4 rounded font-bold hover:bg-[#2D6193] shadow-lg shadow-[#3877AF]/20 flex items-center justify-center gap-2) + secondary text link below (text-[#3877AF] underline text-sm)"
     * "Callout box: bg-[#FEF3C7] border border-[#F59E0B]/30 rounded-lg p-4 mt-8. Has an info icon (AlertCircle or Info from lucide-react in text-[#F59E0B]) + callout text in text-[#92400E] text-sm leading-relaxed"
     * "Section wrapper: py-20 md:py-24 px-5 md:px-10"
     * "Use Framer Motion: whileInView='visible' initial='hidden' viewport={{ once: true, amount: 0.2 }}. Stagger checklist items. Fade-in for CTAs and callout."
   - `framework`: "react"
   - `fileName`: "PackLLC"

3. After receiving Gemini output, wire the logic:
   - Import `useContent` from `../hooks/useContent`
   - Import `motion` from `framer-motion`
   - Import `CheckCircle2, Info` from `lucide-react` (these are fixed icons, not dynamic from content)
   - Import `ArrowRight` from `lucide-react` for CTA arrow
   - Replace hardcoded text with `content.packLLC.*` values
   - Map over `content.packLLC.checklist` array for checklist items
   - Wire `content.packLLC.ctaPrimary.label` and `.href` to primary CTA
   - Wire `content.packLLC.ctaSecondary.label` and `.href` to secondary link
   - Wire `content.packLLC.callout` to callout box text
   - Add `target="_blank" rel="noopener noreferrer"` to CTA links (WhatsApp)
   - Use `whileInView` for scroll-triggered animation
  </action>
  <verify>
    <automated>cd "/Users/enzomaillard/Workspace_Local/Landing Pages/AltixiaLLC" && npx tsc --noEmit src/components/PackLLC.tsx 2>&1 | head -20</automated>
  </verify>
  <done>PackLLC.tsx compiles without errors. Renders section title, subtitle, 7-item checklist with green checkmarks, primary CTA button, secondary WhatsApp link, and amber callout box. Accent blue background. Framer Motion scroll animations. No price displayed. All text bilingual via useContent().</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for PackLLC.tsx
- Section has distinct accent blue background
- 7 checklist items with green checkmark icons
- Primary CTA and secondary WhatsApp link present
- Callout box with amber/yellow styling and info icon
- No price displayed anywhere
- Framer Motion animations trigger on scroll
- All text from useContent() hook
</verification>

<success_criteria>
- PackLLC.tsx created via Gemini MCP with all visual elements
- Accent background distinguishes section
- 7-item checklist with green checkmarks
- CTAs link to WhatsApp
- Amber callout box with "no standalone LLC" explanation
- Scroll-triggered Framer Motion animations
- No TypeScript errors
- All text bilingual
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-content/03-03-SUMMARY.md`
</output>

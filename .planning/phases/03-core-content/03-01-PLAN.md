---
phase: 03-core-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Problem.tsx
autonomous: true
requirements: [PROB-01, PROB-02]

must_haves:
  truths:
    - "Problem section displays 6 pain point cards in a 3x2 grid on desktop"
    - "Cards stack to 1 column on mobile"
    - "Each card shows a Lucide icon in a soft blue circle, a title, and a description"
    - "A transition phrase appears below the card grid linking to Altixia's solution"
    - "Cards animate with fade-in + slide-up on scroll via Framer Motion"
    - "All text is bilingual via useContent() hook"
  artifacts:
    - path: "src/components/Problem.tsx"
      provides: "Problem section with 6 cards and transition phrase"
      min_lines: 40
  key_links:
    - from: "src/components/Problem.tsx"
      to: "src/hooks/useContent.ts"
      via: "useContent() hook for bilingual text"
      pattern: "useContent\\(\\)"
    - from: "src/components/Problem.tsx"
      to: "src/components/LucideIcon.tsx"
      via: "LucideIcon for dynamic card icons"
      pattern: "<LucideIcon"
---

<objective>
Build the Problem section component with 6 pain point cards in a responsive grid and a transition phrase at the bottom.

Purpose: This is the first persuasion section — it empathizes with the visitor's frustrations before presenting Altixia's solution. The cards should feel relatable and the transition phrase should create a natural bridge to the Services section.
Output: Problem.tsx component via Gemini MCP with scroll animations and bilingual content.
</objective>

<execution_context>
@/Users/enzomaillard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/enzomaillard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@design-system.md
@src/types/content.ts
@src/hooks/useContent.ts
@src/components/LucideIcon.tsx
@src/components/Hero.tsx
@src/data/content-fr.ts
@BRIEF.md

<interfaces>
From src/types/content.ts:
```typescript
export interface ProblemContent {
  sectionTitle: string
  subtitle: string
  cards: { icon: string; title: string; description: string }[]
  transition: string
}
```

From src/hooks/useContent.ts:
```typescript
export function useContent(): Content
// Access via: content.problem.sectionTitle, content.problem.cards, content.problem.transition
```

From src/components/LucideIcon.tsx:
```typescript
export function LucideIcon({ name, size, className }: { name: string; size?: number; className?: string }): JSX.Element | null
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Problem section via Gemini MCP</name>
  <files>src/components/Problem.tsx</files>
  <action>
1. Read `design-system.md` fully (all content, verbatim).

2. Use Gemini MCP `create_frontend` to generate the Problem component:
   - `designSystem`: Pass the ENTIRE content of `design-system.md`
   - `context`: Include ALL of the following:
     * "Build a Problem section for Altixia LLC landing page"
     * "Section has a centered header: section title (DM Serif Display, text-[#333A49], text-3xl md:text-4xl font-bold) + subtitle (DM Sans, text-[#6B7280], max-w-2xl mx-auto text-lg)"
     * "Below header: 6 cards in a responsive grid — grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8"
     * "Each card: white bg, p-8, rounded-lg, border border-[#E8EDF2], shadow-sm, hover:shadow-md hover:border-[#3877AF]/30 transition-all"
     * "Card content: icon in a w-12 h-12 bg-[#D5E8F8] rounded-full circle (centered), then title (font-serif text-[#333A49] text-lg font-bold mb-3), then description (text-[#6B7280] leading-relaxed text-sm)"
     * "After the card grid: transition phrase in a centered block — text-center text-[#3877AF] font-semibold text-lg mt-12 max-w-2xl mx-auto"
     * "Section wrapper: py-20 md:py-24 px-5 md:px-10, max-w-6xl mx-auto"
     * "Use Framer Motion: section container with whileInView='visible' initial='hidden' viewport={{ once: true, amount: 0.2 }}. containerVariants stagger children 0.1s. Each card and transition phrase uses itemVariants (opacity 0→1, y 20→0, duration 0.5)"
     * "Icons are rendered as a placeholder div — they will be replaced with LucideIcon component"
   - `framework`: "react"
   - `fileName`: "Problem"

3. After receiving Gemini output, wire the logic:
   - Import `useContent` from `../hooks/useContent`
   - Import `LucideIcon` from `./LucideIcon`
   - Import `motion` from `framer-motion`
   - Replace any hardcoded text with `content.problem.*` values
   - Replace icon placeholders with `<LucideIcon name={card.icon} size={24} className="text-[#3877AF]" />`
   - Map over `content.problem.cards` array for card rendering
   - Add `content.problem.transition` as the transition phrase text
   - Use `whileInView` (NOT `animate`) since this is below the fold
   - Define containerVariants and itemVariants matching Hero.tsx pattern but with whileInView trigger
  </action>
  <verify>
    <automated>cd "/Users/enzomaillard/Workspace_Local/Landing Pages/AltixiaLLC" && npx tsc --noEmit src/components/Problem.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Problem.tsx compiles without errors. Renders 6 cards from content.problem.cards with LucideIcon, titles, descriptions. Transition phrase from content.problem.transition below cards. Framer Motion scroll animations with whileInView. All text bilingual via useContent().</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for Problem.tsx
- Component renders section title, subtitle, 6 cards with icons, and transition phrase
- Cards use responsive grid: 1 col mobile, 2 cols tablet, 3 cols desktop
- Framer Motion animations trigger on scroll into view
- All text from useContent() hook (switches with FR/EN toggle)
</verification>

<success_criteria>
- Problem.tsx created via Gemini MCP with all visual elements
- 6 cards with LucideIcon icons, titles, descriptions
- Responsive grid (1→2→3 columns)
- Transition phrase below cards
- Scroll-triggered Framer Motion animations
- No TypeScript errors
- All text bilingual
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-content/03-01-SUMMARY.md`
</output>

---
phase: 03-core-content
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/WhyAltixia.tsx
autonomous: true
requirements: [WHY-01]

must_haves:
  truths:
    - "Why Altixia section displays 6 differentiator cards in a responsive grid"
    - "Each card has a Lucide icon in a soft blue circle, title, and description"
    - "Cards match the same visual style as Problem section cards (unified base card)"
    - "Cards animate with fade-in + slide-up on scroll via Framer Motion"
    - "All text is bilingual via useContent() hook"
  artifacts:
    - path: "src/components/WhyAltixia.tsx"
      provides: "Why Altixia section with 6 differentiator cards"
      min_lines: 40
  key_links:
    - from: "src/components/WhyAltixia.tsx"
      to: "src/hooks/useContent.ts"
      via: "useContent() hook for bilingual text"
      pattern: "useContent\\(\\)"
    - from: "src/components/WhyAltixia.tsx"
      to: "src/components/LucideIcon.tsx"
      via: "LucideIcon for dynamic card icons"
      pattern: "<LucideIcon"
---

<objective>
Build the Why Altixia section with 6 differentiator cards showing what makes Altixia unique (French language, speed, ongoing support, data protection, international expertise, custom service).

Purpose: This section answers the "why you and not someone else?" question. The cards highlight Altixia's unique value propositions and build trust. Same card pattern as Problem section for visual consistency.
Output: WhyAltixia.tsx component via Gemini MCP with scroll animations and bilingual content.
</objective>

<execution_context>
@/Users/enzomaillard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/enzomaillard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@design-system.md
@src/types/content.ts
@src/hooks/useContent.ts
@src/components/LucideIcon.tsx
@src/components/Hero.tsx
@src/data/content-fr.ts
@BRIEF.md

<interfaces>
From src/types/content.ts:
```typescript
export interface WhyAltixiaContent {
  sectionTitle: string
  subtitle: string
  cards: { icon: string; title: string; description: string }[]
}
```

From src/hooks/useContent.ts:
```typescript
export function useContent(): Content
// Access via: content.whyAltixia.sectionTitle, content.whyAltixia.cards
```

From src/components/LucideIcon.tsx:
```typescript
export function LucideIcon({ name, size, className }: { name: string; size?: number; className?: string }): JSX.Element | null
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Why Altixia section via Gemini MCP</name>
  <files>src/components/WhyAltixia.tsx</files>
  <action>
1. Read `design-system.md` fully (all content, verbatim).

2. Use Gemini MCP `create_frontend` to generate the WhyAltixia component:
   - `designSystem`: Pass the ENTIRE content of `design-system.md`
   - `context`: Include ALL of the following:
     * "Build a Why Altixia differentiator section for Altixia LLC landing page"
     * "Section has a centered header: section title (DM Serif Display, text-[#333A49], text-3xl md:text-4xl font-bold) + subtitle (DM Sans, text-[#6B7280], max-w-2xl mx-auto text-lg)"
     * "Below header: 6 cards in a responsive grid — grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8"
     * "Each card: white bg, p-8, rounded-lg, border border-[#E8EDF2], shadow-sm, hover:shadow-md hover:border-[#3877AF]/30 transition-all — SAME style as Problem section cards (unified base card)"
     * "Card content: icon in w-12 h-12 bg-[#D5E8F8] rounded-full circle (centered), title (font-serif text-[#333A49] text-lg font-bold mb-3), description (text-[#6B7280] leading-relaxed text-sm)"
     * "Section wrapper: py-20 md:py-24 px-5 md:px-10, max-w-6xl mx-auto, white background"
     * "Use Framer Motion: whileInView='visible' initial='hidden' viewport={{ once: true, amount: 0.2 }}. containerVariants stagger 0.1s. itemVariants fade-in + slide-up."
     * "Icons are placeholder divs — will be replaced with LucideIcon"
   - `framework`: "react"
   - `fileName`: "WhyAltixia"

3. After receiving Gemini output, wire the logic:
   - Import `useContent` from `../hooks/useContent`
   - Import `LucideIcon` from `./LucideIcon`
   - Import `motion` from `framer-motion`
   - Replace hardcoded text with `content.whyAltixia.*` values
   - Replace icon placeholders with `<LucideIcon name={card.icon} size={24} className="text-[#3877AF]" />`
   - Map over `content.whyAltixia.cards` array for card rendering
   - Use `whileInView` for scroll-triggered animation
  </action>
  <verify>
    <automated>cd "/Users/enzomaillard/Workspace_Local/Landing Pages/AltixiaLLC" && npx tsc --noEmit src/components/WhyAltixia.tsx 2>&1 | head -20</automated>
  </verify>
  <done>WhyAltixia.tsx compiles without errors. Renders 6 differentiator cards from content.whyAltixia.cards with LucideIcon, titles, descriptions. Same card style as Problem section. Responsive grid 1→2→3 columns. Framer Motion scroll animations. All text bilingual via useContent().</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for WhyAltixia.tsx
- Component renders section title, subtitle, 6 cards with icons
- Cards match Problem section visual style (unified base card)
- Responsive grid: 1 col mobile, 2 cols tablet, 3 cols desktop
- Framer Motion animations trigger on scroll
- All text from useContent() hook
</verification>

<success_criteria>
- WhyAltixia.tsx created via Gemini MCP with all visual elements
- 6 differentiator cards with LucideIcon, titles, descriptions
- Same card pattern as Problem section
- Responsive grid (1→2→3 columns)
- Scroll-triggered Framer Motion animations
- No TypeScript errors
- All text bilingual
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-content/03-04-SUMMARY.md`
</output>

---
phase: 03-core-content
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/HowItWorks.tsx
autonomous: true
requirements: [HOW-01, HOW-02]

must_haves:
  truths:
    - "How It Works section displays a 5-step timeline/stepper"
    - "Stepper is horizontal on desktop with connector lines between steps"
    - "Stepper is vertical on mobile with steps stacked"
    - "Each step has a numbered circle in brand blue (#3877AF), title, and description"
    - "Steps animate sequentially on scroll with staggered delay"
    - "All text is bilingual via useContent() hook"
  artifacts:
    - path: "src/components/HowItWorks.tsx"
      provides: "How It Works stepper with 5 steps, responsive layout"
      min_lines: 50
  key_links:
    - from: "src/components/HowItWorks.tsx"
      to: "src/hooks/useContent.ts"
      via: "useContent() hook for bilingual text"
      pattern: "useContent\\(\\)"
---

<objective>
Build the How It Works section with a 5-step timeline/stepper that renders horizontally on desktop and vertically on mobile, with sequential scroll animation.

Purpose: This section demystifies the LLC creation process — turning a complex procedure into 5 clear steps. The horizontal timeline on desktop feels like a natural progression; vertical on mobile maintains readability.
Output: HowItWorks.tsx component via Gemini MCP with responsive layout and scroll animations.
</objective>

<execution_context>
@/Users/enzomaillard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/enzomaillard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@design-system.md
@src/types/content.ts
@src/hooks/useContent.ts
@src/components/Hero.tsx
@src/data/content-fr.ts
@BRIEF.md

<interfaces>
From src/types/content.ts:
```typescript
export interface HowItWorksContent {
  sectionTitle: string
  subtitle: string
  steps: { number: number; title: string; description: string }[]
}
```

From src/hooks/useContent.ts:
```typescript
export function useContent(): Content
// Access via: content.howItWorks.sectionTitle, content.howItWorks.steps
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create How It Works stepper via Gemini MCP</name>
  <files>src/components/HowItWorks.tsx</files>
  <action>
1. Read `design-system.md` fully (all content, verbatim).

2. Use Gemini MCP `create_frontend` to generate the HowItWorks component:
   - `designSystem`: Pass the ENTIRE content of `design-system.md`
   - `context`: Include ALL of the following:
     * "Build a How It Works 5-step timeline/stepper section for Altixia LLC landing page"
     * "Section has a centered header: section title (DM Serif Display, text-[#333A49], text-3xl md:text-4xl font-bold) + subtitle (DM Sans, text-[#6B7280], max-w-2xl mx-auto text-lg)"
     * "DESKTOP LAYOUT (md: and above): Horizontal timeline with 5 steps in a flex row. Each step is a flex-col items-center text-center block. Between each step pair is a horizontal connector line (h-0.5 bg-[#3877AF]/20 flex-1 mx-2, self-center)."
     * "MOBILE LAYOUT (below md:): Vertical timeline with steps stacked. Each step is a flex-row with the numbered circle on the left, text content on the right. Between each step pair is a vertical connector line (w-0.5 h-8 bg-[#3877AF]/20 ml-7, centered under the circle)."
     * "Step circle: w-14 h-14 bg-[#3877AF] text-white rounded-full flex items-center justify-center text-xl font-bold shrink-0"
     * "Step text: title (font-serif text-[#333A49] font-bold text-base mb-1) + description (text-[#6B7280] text-sm leading-relaxed max-w-[200px] on desktop)"
     * "Section wrapper: py-20 md:py-24 px-5 md:px-10, max-w-6xl mx-auto, light gray background bg-[#F8F9FA]"
     * "Use Framer Motion: whileInView='visible' initial='hidden' viewport={{ once: true, amount: 0.3 }}. containerVariants with staggerChildren: 0.15 for sequential reveal. itemVariants: opacity 0→1, y 30→0, duration 0.6"
   - `framework`: "react"
   - `fileName`: "HowItWorks"

3. After receiving Gemini output, wire the logic:
   - Import `useContent` from `../hooks/useContent`
   - Import `motion` from `framer-motion`
   - Replace hardcoded step data with `content.howItWorks.steps` mapping
   - Replace hardcoded title/subtitle with `content.howItWorks.sectionTitle` and `.subtitle`
   - Use `step.number` for the numbered circle
   - Use `step.title` and `step.description` for text
   - Ensure desktop layout uses `hidden md:flex` for horizontal and `flex md:hidden` for vertical
   - Use `whileInView` with higher stagger (0.15s) for sequential feel
   - Connector lines: render between steps (not after the last step) using `i < steps.length - 1` check
  </action>
  <verify>
    <automated>cd "/Users/enzomaillard/Workspace_Local/Landing Pages/AltixiaLLC" && npx tsc --noEmit src/components/HowItWorks.tsx 2>&1 | head -20</automated>
  </verify>
  <done>HowItWorks.tsx compiles without errors. Renders 5 steps with numbered circles, titles, descriptions, and connector lines. Horizontal on desktop, vertical on mobile. Steps animate sequentially on scroll. All text bilingual via useContent().</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for HowItWorks.tsx
- 5 steps with numbered circles in brand blue (#3877AF)
- Horizontal layout on desktop with connector lines
- Vertical layout on mobile
- Sequential scroll animation with staggered delay
- All text from useContent() hook
</verification>

<success_criteria>
- HowItWorks.tsx created via Gemini MCP
- 5-step timeline with numbered circles and connectors
- Responsive: horizontal desktop, vertical mobile
- Sequential Framer Motion scroll animation
- No TypeScript errors
- All text bilingual
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-content/03-05-SUMMARY.md`
</output>

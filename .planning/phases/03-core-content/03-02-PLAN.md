---
phase: 03-core-content
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Services.tsx
autonomous: true
requirements: [SERV-01, SERV-02]

must_haves:
  truths:
    - "Services section displays 8 service cards in a responsive grid (4x2 on desktop)"
    - "Each card shows a Lucide icon in a soft blue circle, title, description, and a pill-shaped badge"
    - "Badges use pill shape matching the Hero badge pattern (rounded-full, colored background)"
    - "Cards animate with fade-in + slide-up on scroll via Framer Motion"
    - "Section has light gray (#F8F9FA) background for visual rhythm"
    - "All text is bilingual via useContent() hook"
  artifacts:
    - path: "src/components/Services.tsx"
      provides: "Services section with 8 cards and badges"
      min_lines: 40
  key_links:
    - from: "src/components/Services.tsx"
      to: "src/hooks/useContent.ts"
      via: "useContent() hook for bilingual text"
      pattern: "useContent\\(\\)"
    - from: "src/components/Services.tsx"
      to: "src/components/LucideIcon.tsx"
      via: "LucideIcon for dynamic card icons"
      pattern: "<LucideIcon"
---

<objective>
Build the Services section component with 8 service cards in a responsive grid, each with a Lucide icon, title, description, and pill-shaped badge.

Purpose: This section showcases all Altixia services — from LLC creation to IT services. The badges highlight key selling points (Express 48h, Included in pack, etc.) and the 4x2 grid provides a comprehensive overview at a glance.
Output: Services.tsx component via Gemini MCP with scroll animations and bilingual content.
</objective>

<execution_context>
@/Users/enzomaillard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/enzomaillard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@design-system.md
@src/types/content.ts
@src/hooks/useContent.ts
@src/components/LucideIcon.tsx
@src/components/Hero.tsx
@src/data/content-fr.ts
@BRIEF.md

<interfaces>
From src/types/content.ts:
```typescript
export interface ServicesContent {
  sectionTitle: string
  subtitle: string
  cards: { icon: string; title: string; description: string; badge: string }[]
}
```

From src/hooks/useContent.ts:
```typescript
export function useContent(): Content
// Access via: content.services.sectionTitle, content.services.cards
```

From src/components/LucideIcon.tsx:
```typescript
export function LucideIcon({ name, size, className }: { name: string; size?: number; className?: string }): JSX.Element | null
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Services section via Gemini MCP</name>
  <files>src/components/Services.tsx</files>
  <action>
1. Read `design-system.md` fully (all content, verbatim).

2. Use Gemini MCP `create_frontend` to generate the Services component:
   - `designSystem`: Pass the ENTIRE content of `design-system.md`
   - `context`: Include ALL of the following:
     * "Build a Services section for Altixia LLC landing page"
     * "Section has a centered header: section title (DM Serif Display, text-[#333A49], text-3xl md:text-4xl font-bold) + subtitle (DM Sans, text-[#6B7280], max-w-2xl mx-auto text-lg)"
     * "Below header: 8 cards in a responsive grid — grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6"
     * "Each card: white bg, p-6 md:p-8, rounded-lg, border border-[#E8EDF2], shadow-sm, hover:shadow-md hover:border-[#3877AF]/30 transition-all"
     * "Card layout (top to bottom): icon in w-12 h-12 bg-[#D5E8F8] rounded-full circle, title (font-serif text-[#333A49] text-lg font-bold mb-2), description (text-[#6B7280] text-sm leading-relaxed mb-4), badge pill at bottom"
     * "Badge pill: inline-flex px-3 py-1 rounded-full text-xs font-bold bg-[#D5E8F8] text-[#3877AF] — matching the Hero badge pattern"
     * "Section wrapper: py-20 md:py-24 px-5 md:px-10, max-w-6xl mx-auto"
     * "Use Framer Motion: whileInView='visible' initial='hidden' viewport={{ once: true, amount: 0.1 }}. containerVariants stagger 0.08s. itemVariants fade-in + slide-up."
     * "Icons are placeholder divs — will be replaced with LucideIcon"
   - `framework`: "react"
   - `fileName`: "Services"

3. After receiving Gemini output, wire the logic:
   - Import `useContent` from `../hooks/useContent`
   - Import `LucideIcon` from `./LucideIcon`
   - Import `motion` from `framer-motion`
   - Replace hardcoded text with `content.services.*` values
   - Replace icon placeholders with `<LucideIcon name={card.icon} size={24} className="text-[#3877AF]" />`
   - Map over `content.services.cards` array for card rendering
   - Render `card.badge` in the pill-shaped badge element
   - Use `whileInView` for scroll-triggered animation
   - Use slightly faster stagger (0.08s) since there are 8 cards
  </action>
  <verify>
    <automated>cd "/Users/enzomaillard/Workspace_Local/Landing Pages/AltixiaLLC" && npx tsc --noEmit src/components/Services.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Services.tsx compiles without errors. Renders 8 service cards from content.services.cards with LucideIcon, titles, descriptions, and pill badges. Responsive grid 1→2→4 columns. Framer Motion scroll animations. All text bilingual via useContent().</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for Services.tsx
- Component renders section title, subtitle, 8 cards with icons and badges
- Cards use responsive grid: 1 col mobile, 2 cols tablet, 4 cols desktop
- Each card has a pill-shaped badge
- Framer Motion animations trigger on scroll into view
- All text from useContent() hook
</verification>

<success_criteria>
- Services.tsx created via Gemini MCP with all visual elements
- 8 cards with LucideIcon icons, titles, descriptions, and badges
- Responsive grid (1→2→4 columns)
- Pill badges matching Hero badge pattern
- Scroll-triggered Framer Motion animations
- No TypeScript errors
- All text bilingual
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-content/03-02-SUMMARY.md`
</output>
